[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vibe-guide"
version = "4.8.0"
requires-python = ">=3.9"

[project.scripts]
vibe = "vibe_guide.cli:main"
