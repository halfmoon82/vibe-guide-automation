[build-system]
requires = ["setuptools>=58", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vibe-guide"
version = "4.2.2"
requires-python = ">=3.9"

[project.scripts]
vibe = "vibe_guide.cli:main"
